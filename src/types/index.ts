export type Article = {
  title: string;
  description: string;
  category: string;
  file: FileList;
};

export type ArticleTest = {
  title: string;
  description: string;
  category: string;
  file: string;
};
